@{
    ViewBag.Title = "Product";
}

<h2>Product</h2>
<p>Id: @ViewBag.Id</p>
<div id="maindiv">

</div>
<div>
    <h3>VueJS test</h3>
    <div id="app">
        {{ info }}
        <ul>
            <li v-for="b in bears">{{b.name}} is {{b.color}}!</li>
        </ul>
    </div>
    
</div>

@section scripts{
    @Scripts.Render("~/bundles/VueJS")
    <script>
        var TestData = {
            Name: 'Товар 1',
            Description: 'Очень важный товар',
            Price: 800,
            Count: 10,
            PhotoSrc: 'https://www.aviary.com/img/photo-landscape.jpg',
            Specifications: 'Ширина=5мм;Высота=10мм;Радиус=100мм;WiFi=есть;Количество USB портов=5-6'
        };
        var TestComments = [{
            AuthorName: 'Саша',
            Text: 'Очень классный товар. Всем рекомендую))))0))00)'
        }, {
            AuthorName: 'Дима',
            Text: 'Ужас. Просто ужасная вещь. Сломалась от падения на пол'
        }, {
            AuthorName: 'Света1999',
            Text: 'Хз, вродь все норм'
        }];

        var data = {
            bears: [
                { name: 'Misha', color: 'White' },
                { name: 'Dima', color: 'Black' },
                { name: 'Borya', color: 'Brown' },
                { name: 'Vasya', color: 'Yellow' }
            ],
            info: 'Hello!'
        }
        var TestVue = new Vue({
            el: '#app',
            data: data
        });

        var xhr = new XMLHttpRequest();
        xhr.open('GET', (window.location.pathname + '/data'), true);
        xhr.send();
        xhr.onreadystatechange = function () {
            if (xhr.status == 200 && xhr.readyState == 4) {
                //xhr.responseText;
                document.getElementById('maindiv').innerHTML = xhr.responseText;
            }
        }
    </script>
}
